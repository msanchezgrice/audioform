# @talkform/db

This private package holds the durable commercial schema. Apply the migration only to a Talkform-owned Postgres database after provider, region, backup, and data-residency choices are confirmed.

The public demo and the local MCP server do not depend on this database.
