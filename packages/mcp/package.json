{
  "name": "@talkform/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "talkform-mcp": "./src/server.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.1",
    "@talkform/core": "workspace:*",
    "tsx": "^4.21.0",
    "zod": "^3.24.4"
  }
}
