import { respondentToken } from "@/lib/platform/auth";
import { authenticateRespondentHandoff, getRespondentHandoff, submitRespondentHandoff } from "@/lib/platform/handoffs";
import { consumePlatformRateLimit, platformErrorResponse, platformJson, readJson, requireUuid } from "../../_lib/http";
export const runtime = "nodejs";
export async function GET(request:Request,{params}:{params:Promise<{handoffId:string}>}) { try { const {handoffId}=await params; const id=requireUuid(handoffId); const token=respondentToken(request); const actor=await authenticateRespondentHandoff(id,token); await consumePlatformRateLimit(request,actor.actorKey,120); const handoff=await getRespondentHandoff(id,token); return platformJson({config:handoff.config,status:handoff.status}); } catch(error){ return platformErrorResponse(error); } }
export async function POST(request:Request,{params}:{params:Promise<{handoffId:string}>}) { try { const {handoffId}=await params; const id=requireUuid(handoffId); const token=respondentToken(request); const actor=await authenticateRespondentHandoff(id,token); await consumePlatformRateLimit(request,actor.actorKey,60); return platformJson(await submitRespondentHandoff(id,token,await readJson(request))); } catch(error){ return platformErrorResponse(error); } }
